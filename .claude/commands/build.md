Compile the TypeScript project:

1. Run `npm run build`
2. Report any compilation errors
3. If errors exist, read the failing files and fix the type errors
4. Re-run build until clean
5. Run `npx prettier --write 'src/**/*.ts' 'tests/**/*.ts'` to fix formatting
6. Report: files compiled, any warnings
