Run the full test suite:

1. Run `npm test`
2. If any tests fail, analyze the failures
3. Read the failing test files and the source files they test
4. Fix the root cause in the source code (not the test) unless the test itself is wrong
5. Re-run tests until all pass
6. Report: total tests, passed, failed, duration
